# Phase 05 Checkpoint — RAG + Knowledge

```yaml
phase: "PHASE 05"
status: "COMPLETED"
objective: "Crear una capa de conocimiento genérica y aislada por tenant"
note: "Checkpoint generado retroactivamente el 2026-08-02 con evidencia verificada del código y de la app en ejecución (AI-BOS-STATE ya marcaba la fase como COMPLETED)."

implemented:
  - "pgvector extension habilitado en Supabase"
  - "Tablas knowledge_bases, documents, document_chunks con tenant_id y RLS"
  - "Función match_document_chunks para búsqueda por similitud vectorial"
  - "Pipeline de chunking (chunker.ts)"
  - "Generación de embeddings vía OpenRouter (embeddings.ts)"
  - "Pipeline de ingesta de documentos (ingest.ts)"
  - "Servicio de retrieval con filtros por tenant y knowledge base (retrieval.ts)"
  - "Scraper de URLs (url-scraper.ts)"
  - "API routes: knowledge-bases CRUD, documents list/upload/delete"
  - "UI de Knowledge en el dashboard: gestión de KBs, documentos, drag-and-drop upload, status indicators"
  - "Contexto RAG inyectado en el system prompt del LLM con fallback cuando no hay documentos"
  - "Separación estricta: contenido RAG no se convierte en instrucciones del sistema"

files_created:
  - "src/infrastructure/database/migrations/005_rag_tables.sql"
  - "src/core/types/rag.ts"
  - "src/lib/rag/chunker.ts"
  - "src/lib/rag/embeddings.ts"
  - "src/lib/rag/ingest.ts"
  - "src/lib/rag/retrieval.ts"
  - "src/lib/rag/url-scraper.ts"
  - "src/app/api/knowledge-bases/route.ts"
  - "src/app/api/knowledge-bases/[id]/route.ts"
  - "src/app/api/knowledge-bases/[id]/documents/route.ts"
  - "src/app/api/documents/[id]/route.ts"
  - "src/app/api/documents/[id]/upload/route.ts"
  - "src/hooks/use-knowledge-bases.ts"
  - "src/hooks/use-documents.ts"
  - "src/components/UploadZone.tsx"
  - "src/components/DocumentList.tsx"
  - "src/components/KnowledgeBaseCard.tsx"
  - "src/components/KnowledgeSection.tsx"

database_changes:
  - "CREATE EXTENSION vector"
  - "CREATE TABLE knowledge_bases (id, tenant_id, name, description, ...)"
  - "CREATE TABLE documents (id, tenant_id, kb_id, status, file_type, ...)"
  - "CREATE TABLE document_chunks (id, tenant_id, document_id, chunk_index, content, embedding vector, ...)"
  - "CREATE POLICY kb_tenant_isolation / docs_tenant_isolation / chunks_tenant_isolation"
  - "CREATE FUNCTION match_document_chunks (búsqueda por similitud coseno con filtro tenant/kb)"

tests:
  executed: 15
  passed: 15
  failed: 0

validation: "Typecheck sin errores. 15 tests pasando. App verificada en ejecución: bot conectado, dashboard respondiendo en localhost:3000, estado Redis connected."

evidence:
  - "npm run typecheck — 0 errores (2026-08-02)"
  - "npm run test — 15/15 tests pasando (2026-08-02)"
  - "docker ps — whatsapp-agent Up"
  - "docker logs whatsapp-agent — ✓ conectado como 5215664436277, outbox listener activo"
  - "GET /api/connection/status — { status: connected, phone: 5215664436277 }"
  - "src/infrastructure/database/migrations/005_rag_tables.sql"
  - "src/lib/rag/*.ts (chunker, embeddings, ingest, retrieval)"

known_issues:
  - "El checkpoint de fase 05 no se había generado previamente; se creó retroactivamente con evidencia del código."

risks:
  - "R-006 RESUELTO: n8n sube documentos vacíos (bug MIME) → typeVersion 3, patch verificado E2E"

blockers: []

master_version: "2.0.0"

ready_for_next_phase: true

human_approval_required: true
```

---

## Validation Checklist

- [x] pgvector habilitado
- [x] Múltiples knowledge bases por tenant
- [x] Pipeline INGEST → CLEAN → CHUNK → METADATA → EMBEDDING → STORE
- [x] Pipeline QUERY → EMBED → FILTER → SEARCH → RANK → CONTEXT
- [x] Separación entre system instructions y business knowledge
- [x] Filtros por tenant y knowledge base
- [x] UI de Knowledge en dashboard
- [x] Fallback cuando no hay documentos
- [x] TypeScript compila sin errores
- [x] Tests pasando
- [x] App verificada en ejecución (bot + dashboard)

---

## Arquitectura Verificada

```text
TENANT
↓
KNOWLEDGE BASE
↓
INGESTION (ingest.ts)
↓
CHUNKING (chunker.ts)
↓
METADATA
↓
EMBEDDING (embeddings.ts via OpenRouter)
↓
VECTOR STORE (pgvector)
↓
RETRIEVAL (retrieval.ts)
↓
RANKING (match_document_chunks)
↓
CONTEXT (inyectado en system prompt del LLM)
```

---

**Generated by:** AI-BOS Phase 05 — RAG + Knowledge
**Date:** 2026-08-02 (checkpoint retroactivo, validación en vivo)
**Status:** COMPLETED — verificado
